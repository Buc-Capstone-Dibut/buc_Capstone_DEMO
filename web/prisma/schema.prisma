generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema", "driverAdapters"]
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
  schemas   = ["auth", "public"]
}

// Canonical workspace schema:
// update workspace-domain models here first, then align workspace-server afterward.

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                @id @db.Uuid
  user_id                String?               @db.Uuid
  auth_code              String
  code_challenge_method  code_challenge_method
  code_challenge         String
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?             @db.Timestamptz(6)
  updated_at             DateTime?             @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?             @db.Timestamptz(6)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String    @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
  email           String?   @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 users?                     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                   String                  @id @db.Uuid
  client_secret_hash   String?
  registration_type    oauth_registration_type
  redirect_uris        String
  grant_types          String
  client_name          String?
  client_uri           String?
  logo_uri             String?
  created_at           DateTime                @default(now()) @db.Timestamptz(6)
  updated_at           DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at           DateTime?               @db.Timestamptz(6)
  client_type          oauth_client_type       @default(confidential)
  oauth_authorizations oauth_authorizations[]
  oauth_consents       oauth_consents[]
  sessions             sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      users               @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model users {
  instance_id                 String?                @db.Uuid
  id                          String                 @id @db.Uuid
  aud                         String?                @db.VarChar(255)
  role                        String?                @db.VarChar(255)
  email                       String?                @db.VarChar(255)
  encrypted_password          String?                @db.VarChar(255)
  email_confirmed_at          DateTime?              @db.Timestamptz(6)
  invited_at                  DateTime?              @db.Timestamptz(6)
  confirmation_token          String?                @db.VarChar(255)
  confirmation_sent_at        DateTime?              @db.Timestamptz(6)
  recovery_token              String?                @db.VarChar(255)
  recovery_sent_at            DateTime?              @db.Timestamptz(6)
  email_change_token_new      String?                @db.VarChar(255)
  email_change                String?                @db.VarChar(255)
  email_change_sent_at        DateTime?              @db.Timestamptz(6)
  last_sign_in_at             DateTime?              @db.Timestamptz(6)
  raw_app_meta_data           Json?
  raw_user_meta_data          Json?
  is_super_admin              Boolean?
  created_at                  DateTime?              @db.Timestamptz(6)
  updated_at                  DateTime?              @db.Timestamptz(6)
  phone                       String?                @unique
  phone_confirmed_at          DateTime?              @db.Timestamptz(6)
  phone_change                String?                @default("")
  phone_change_token          String?                @default("") @db.VarChar(255)
  phone_change_sent_at        DateTime?              @db.Timestamptz(6)
  confirmed_at                DateTime?              @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current  String?                @default("") @db.VarChar(255)
  email_change_confirm_status Int?                   @default(0) @db.SmallInt
  banned_until                DateTime?              @db.Timestamptz(6)
  reauthentication_token      String?                @default("") @db.VarChar(255)
  reauthentication_sent_at    DateTime?              @db.Timestamptz(6)
  is_sso_user                 Boolean                @default(false)
  deleted_at                  DateTime?              @db.Timestamptz(6)
  is_anonymous                Boolean                @default(false)
  identities                  identities[]
  mfa_factors                 mfa_factors[]
  oauth_authorizations        oauth_authorizations[]
  oauth_consents              oauth_consents[]
  one_time_tokens             one_time_tokens[]
  sessions                    sessions[]
  profiles                    profiles?

  @@index([instance_id])
  @@index([is_anonymous])
  @@schema("auth")
}

model blogs {
  id            Int              @id @default(autoincrement())
  title         String           @db.VarChar(500)
  summary       String?
  author        String           @db.VarChar(100)
  tags          String[]         @default([])
  published_at  DateTime?        @default(now()) @db.Timestamptz(6)
  thumbnail_url String?
  external_url  String
  views         Int?             @default(0)
  created_at    DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?        @default(now()) @db.Timestamptz(6)
  blog_type     String?          @default("company") @db.VarChar(50)
  category      String?          @db.VarChar(50)
  bookmarks     blog_bookmarks[]

  @@index([external_url], map: "idx_blogs_external_url")
  @@index([published_at(sort: Desc)], map: "idx_blogs_published_at")
  @@index([tags], map: "idx_blogs_tags", type: Gin)
  @@index([views(sort: Desc)], map: "idx_blogs_views")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model comments {
  id             String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  post_id        String?    @db.Uuid
  author_id      String?    @db.Uuid
  content        String
  parent_id      String?    @db.Uuid
  is_accepted    Boolean?   @default(false)
  created_at     DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?  @default(now()) @db.Timestamptz(6)
  profiles       profiles?  @relation(fields: [author_id], references: [id], onUpdate: NoAction)
  comments       comments?  @relation("commentsTocomments", fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_comments comments[] @relation("commentsTocomments")
  posts          posts?     @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([author_id, created_at(sort: Desc)])
  @@index([post_id, created_at(sort: Desc)])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model squad_comments {
  id                   String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  squad_id             String           @db.Uuid
  author_id            String?          @db.Uuid
  content              String
  parent_id            String?          @db.Uuid
  created_at           DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?        @default(now()) @db.Timestamptz(6)
  profiles             profiles?        @relation(fields: [author_id], references: [id], onDelete: SetNull, onUpdate: NoAction)
  squads               squads           @relation(fields: [squad_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  squad_comments       squad_comments?  @relation("squad_commentsTosquad_comments", fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_squad_comments squad_comments[] @relation("squad_commentsTosquad_comments")

  @@index([parent_id], map: "idx_squad_comments_parent_id")
  @@index([squad_id, created_at], map: "idx_squad_comments_squad_created_at")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model posts {
  id                  String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  author_id           String?    @db.Uuid
  title               String
  content             String
  category            String
  tags                String[]   @default([])
  views               Int?       @default(0)
  likes               Int?       @default(0)
  has_accepted_answer Boolean?   @default(false)
  created_at          DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?  @default(now()) @db.Timestamptz(6)
  comments            comments[]
  profiles            profiles?  @relation(fields: [author_id], references: [id], onUpdate: NoAction)

  @@index([author_id, created_at(sort: Desc)])
  @@index([category, created_at(sort: Desc)])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model profiles {
  id                 String               @id @db.Uuid
  handle             String               @unique
  nickname           String?
  avatar_url         String?
  bio                String?
  reputation         Int?                 @default(0)
  tier               String?              @default("씨앗")
  tech_stack         String[]
  created_at         DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?            @default(now()) @db.Timestamptz(6)
  comments           comments[]
  posts              posts[]
  squad_comments     squad_comments[]
  users              users                @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  squad_applications squad_applications[]
  squad_members      squad_members[]
  squads             squads[]

  // Workspace & Notification Relations
  workspace_members  workspace_members[]
  workspace_invites  workspace_invites[]
  workspace_docs     workspace_docs[]
  started_doc_collab_sessions workspace_doc_collab_sessions[] @relation("WorkspaceDocCollabStartedBy")
  workspace_doc_live_presence workspace_doc_live_presence[]
  workspace_doc_templates_created workspace_doc_templates[] @relation("WorkspaceDocTemplateCreator")
  workspace_doc_assets_created workspace_doc_assets[] @relation("WorkspaceDocAssetCreator")
  workspace_doc_comments_authored workspace_doc_comments[] @relation("WorkspaceDocCommentAuthor")
  workspace_doc_comments_resolved workspace_doc_comments[] @relation("WorkspaceDocCommentResolver")
  created_task_document_links kanban_task_documents[] @relation("WorkspaceTaskDocumentCreator")
  kanban_tasks       kanban_tasks[]
  notifications      notifications[]
  messages           workspace_messages[]
  blog_bookmarks     blog_bookmarks[]
  resume_profile     user_resume_profiles?
  workspace_settings user_workspace_settings?
  activity_events    user_activity_events[]
  reputation_events  reputation_events[]
  resumes            user_resumes[]

  @@schema("public")
}

model user_resumes {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id          String   @db.Uuid
  title            String   @default("새 이력서")
  resume_payload   Json
  public_summary   Json
  is_active        Boolean  @default(false)
  source_type      String   @default("manual")
  source_file_name String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  updated_at       DateTime @default(now()) @updatedAt @db.Timestamptz(6)
  profiles         profiles @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id, created_at(sort: Desc)])
  @@schema("public")
}

model user_resume_profiles {
  user_id          String   @id @db.Uuid
  resume_payload   Json
  public_summary   Json
  source_type      String   @default("manual")
  source_file_name String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  updated_at       DateTime @default(now()) @updatedAt @db.Timestamptz(6)
  profiles         profiles @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model user_workspace_settings {
  user_id          String   @id @db.Uuid
  settings_payload Json
  public_summary   Json
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  updated_at       DateTime @default(now()) @updatedAt @db.Timestamptz(6)
  profiles         profiles @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model blog_bookmarks {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id    String   @db.Uuid
  blog_id    Int
  created_at DateTime @default(now()) @db.Timestamptz(6)
  profiles   profiles @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  blogs      blogs    @relation(fields: [blog_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, blog_id])
  @@index([user_id, created_at(sort: Desc)])
  @@schema("public")
}

model user_activity_events {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id    String   @db.Uuid
  event_type String
  ref_id     String?
  created_at DateTime @default(now()) @db.Timestamptz(6)
  profiles   profiles @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id, created_at(sort: Desc)])
  @@index([event_type])
  @@index([user_id, event_type, created_at(sort: Desc)])
  @@schema("public")
}

model reputation_events {
  id         String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id    String    @db.Uuid
  event_type String
  delta      Int
  source_type String?
  source_id  String?
  actor_id   String?   @db.Uuid
  dedupe_key String?   @unique
  metadata   Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  profiles   profiles  @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id, created_at(sort: Desc)])
  @@index([event_type, created_at(sort: Desc)])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model squad_applications {
  id         String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  squad_id   String?   @db.Uuid
  user_id    String?   @db.Uuid
  message    String?
  status     String?   @default("pending")
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  squads     squads?   @relation(fields: [squad_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles   profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([squad_id, user_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model squad_members {
  id        String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  squad_id  String?   @db.Uuid
  user_id   String?   @db.Uuid
  role      String?   @default("member")
  joined_at DateTime? @default(now()) @db.Timestamptz(6)
  squads    squads?   @relation(fields: [squad_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles  profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([squad_id, user_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

// --- WORKSPACE SYSTEM ---

model workspaces {
  id          String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name        String
  description String?
  icon_url    String?
  category    String  @default("project")
  space_status workspace_space_status @default(ACTIVE)
  activated_at DateTime?
  lifecycle_status workspace_lifecycle_status @default(IN_PROGRESS)
  completed_at     DateTime?
  result_type      String?
  result_link      String?
  result_note      String?

  // Optional link to origin squad
  from_squad_id String? @unique @db.Uuid

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  members workspace_members[]
  invites workspace_invites[]

  columns    kanban_columns[]
  tags       kanban_tags[]
  views      workspace_views[]
  docs       workspace_docs[]
  doc_collab_sessions workspace_doc_collab_sessions[]
  doc_live_presence workspace_doc_live_presence[]
  doc_templates workspace_doc_templates[]
  doc_assets workspace_doc_assets[]
  task_document_links kanban_task_documents[]
  doc_comments workspace_doc_comments[]
  channels   workspace_channels[]
  whiteboard workspace_whiteboards?

  squad squads? @relation(fields: [from_squad_id], references: [id])

  @@schema("public")
}

enum workspace_space_status {
  DRAFT
  ACTIVE

  @@schema("public")
}

enum workspace_lifecycle_status {
  IN_PROGRESS
  COMPLETED

  @@schema("public")
}

model workspace_members {
  id           String @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String @db.Uuid
  user_id      String @db.Uuid

  role      String   @default("member") // owner, admin, member, viewer
  team_role String?
  joined_at DateTime @default(now())

  workspace workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  user      profiles   @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@unique([workspace_id, user_id])
  @@index([user_id])
  @@schema("public")
}

model workspace_invites {
  id           String @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String @db.Uuid
  email        String
  token        String @unique
  role         String @default("member")
  team_role    String?
  inviter_id   String @db.Uuid

  expires_at DateTime
  created_at DateTime @default(now())

  workspace workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  inviter   profiles   @relation(fields: [inviter_id], references: [id])

  @@schema("public")
}

// --- FEATURES ---

model kanban_columns {
  id           String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String         @db.Uuid
  title        String
  category     String?        @default("todo")
  order        Int
  tasks        kanban_tasks[]

  workspace workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade)

  @@index([workspace_id, order])
  @@schema("public")
}

model workspace_views {
  id              String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id    String   @db.Uuid
  name            String
  type            String   @default("kanban")
  group_by        String
  icon            String?
  color           String?
  columns         Json?
  card_properties Json?
  filters         Json?
  show_empty_groups Boolean @default(true)
  column_order    Json?
  is_system       Boolean  @default(false)
  view_order      Int      @default(0)
  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  workspace workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade)

  @@index([workspace_id, view_order])
  @@schema("public")
}

model kanban_tags {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String   @db.Uuid
  name         String
  color        String
  created_at   DateTime @default(now())

  workspace workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade)

  @@unique([workspace_id, name])
  @@schema("public")
}

model kanban_tasks {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  column_id   String    @db.Uuid
  title       String
  description String?
  assignee_id String?   @db.Uuid
  priority    String?   @default("medium")
  tags        String[]
  order       Int
  due_date    DateTime?
  created_at  DateTime  @default(now())
  updated_at  DateTime  @updatedAt

  column   kanban_columns @relation(fields: [column_id], references: [id], onDelete: Cascade)
  assignee profiles?      @relation(fields: [assignee_id], references: [id])
  documents kanban_task_documents[]

  @@index([column_id, order])
  @@index([assignee_id])
  @@schema("public")
}

model kanban_task_documents {
  id            String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id  String   @db.Uuid
  task_id       String   @db.Uuid
  doc_id        String   @db.Uuid
  relation_type String   @default("reference")
  is_primary    Boolean  @default(false)
  created_by    String   @db.Uuid
  created_at    DateTime @default(now())

  workspace workspaces     @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  task      kanban_tasks   @relation(fields: [task_id], references: [id], onDelete: Cascade)
  doc       workspace_docs @relation(fields: [doc_id], references: [id], onDelete: Cascade)
  creator   profiles       @relation("WorkspaceTaskDocumentCreator", fields: [created_by], references: [id], onDelete: Cascade)

  @@unique([task_id, doc_id])
  @@index([workspace_id, task_id])
  @@index([workspace_id, doc_id])
  @@index([task_id, is_primary])
  @@schema("public")
}

model workspace_docs {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String   @db.Uuid
  kind         String   @default("page")
  title        String
  content      Json?    @db.Json
  emoji        String?
  cover_url    String?
  parent_id    String?  @db.Uuid
  sort_order   Int      @default(0)
  is_archived  Boolean  @default(false)
  author_id    String   @db.Uuid
  created_at   DateTime @default(now())
  updated_at   DateTime @updatedAt

  workspace workspaces       @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  author    profiles         @relation(fields: [author_id], references: [id])
  parent    workspace_docs?  @relation("DocHierarchy", fields: [parent_id], references: [id])
  children  workspace_docs[] @relation("DocHierarchy")
  state     workspace_doc_states?
  collab_session workspace_doc_collab_sessions?
  live_presence workspace_doc_live_presence[]
  templates_based_on workspace_doc_templates[] @relation("WorkspaceDocTemplateSourceDoc")
  assets    workspace_doc_assets[]
  task_links kanban_task_documents[]
  comments  workspace_doc_comments[]

  @@index([workspace_id, updated_at(sort: Desc)])
  @@index([workspace_id, parent_id, is_archived, sort_order])
  @@index([parent_id])
  @@schema("public")
}

model workspace_doc_states {
  doc_id      String   @id @db.Uuid
  yjs_state   String?
  updated_at  DateTime @default(now()) @updatedAt
  updated_by  String?  @db.Uuid

  doc workspace_docs @relation(fields: [doc_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

model workspace_doc_collab_sessions {
  doc_id           String    @id @db.Uuid
  workspace_id     String    @db.Uuid
  status           String    @default("ENDED")
  started_by       String?   @db.Uuid
  started_at       DateTime? @db.Timestamptz(6)
  ended_at         DateTime? @db.Timestamptz(6)
  last_activity_at DateTime? @db.Timestamptz(6)

  doc        workspace_docs @relation(fields: [doc_id], references: [id], onDelete: Cascade)
  workspace  workspaces     @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  started_by_profile profiles? @relation("WorkspaceDocCollabStartedBy", fields: [started_by], references: [id], onDelete: SetNull)

  @@index([workspace_id, status, last_activity_at(sort: Desc)])
  @@schema("public")
}

model workspace_doc_live_presence {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String   @db.Uuid
  doc_id       String   @db.Uuid
  user_id      String   @db.Uuid
  mode         String   @default("NORMAL")
  is_dirty     Boolean  @default(false)
  last_seen_at DateTime @default(now()) @db.Timestamptz(6)

  workspace workspaces     @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  doc       workspace_docs @relation(fields: [doc_id], references: [id], onDelete: Cascade)
  user      profiles       @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@unique([doc_id, user_id])
  @@index([workspace_id, doc_id, mode, last_seen_at(sort: Desc)])
  @@index([user_id, last_seen_at(sort: Desc)])
  @@schema("public")
}

model workspace_doc_assets {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String   @db.Uuid
  doc_id       String   @db.Uuid
  storage_path String
  mime_type    String
  size_bytes   Int
  created_by   String   @db.Uuid
  created_at   DateTime @default(now())

  workspace workspaces     @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  doc       workspace_docs @relation(fields: [doc_id], references: [id], onDelete: Cascade)
  creator   profiles       @relation("WorkspaceDocAssetCreator", fields: [created_by], references: [id], onDelete: Cascade)

  @@index([workspace_id, doc_id, created_at(sort: Desc)])
  @@index([doc_id, created_at(sort: Desc)])
  @@schema("public")
}

model workspace_doc_templates {
  id            String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id  String   @db.Uuid
  created_by    String   @db.Uuid
  source_doc_id String?  @db.Uuid
  name          String
  description   String?
  emoji         String?
  title         String
  content       Json?    @db.Json
  created_at    DateTime @default(now())
  updated_at    DateTime @updatedAt

  workspace workspaces      @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  creator   profiles        @relation("WorkspaceDocTemplateCreator", fields: [created_by], references: [id], onDelete: Cascade)
  source_doc workspace_docs? @relation("WorkspaceDocTemplateSourceDoc", fields: [source_doc_id], references: [id], onDelete: SetNull)

  @@index([workspace_id, updated_at(sort: Desc)])
  @@index([workspace_id, name])
  @@index([source_doc_id])
  @@schema("public")
}

model workspace_doc_comments {
  id              String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id    String   @db.Uuid
  doc_id          String   @db.Uuid
  author_id       String   @db.Uuid
  parent_id       String?  @db.Uuid
  content         String
  anchor_block_id String?
  resolved_at     DateTime?
  resolved_by     String?  @db.Uuid
  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  workspace workspaces               @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  doc       workspace_docs           @relation(fields: [doc_id], references: [id], onDelete: Cascade)
  author    profiles                 @relation("WorkspaceDocCommentAuthor", fields: [author_id], references: [id], onDelete: Cascade)
  resolver  profiles?                @relation("WorkspaceDocCommentResolver", fields: [resolved_by], references: [id], onDelete: SetNull)
  parent    workspace_doc_comments?  @relation("WorkspaceDocCommentThread", fields: [parent_id], references: [id], onDelete: Cascade)
  replies   workspace_doc_comments[] @relation("WorkspaceDocCommentThread")

  @@index([workspace_id, doc_id, parent_id, created_at])
  @@index([doc_id, created_at])
  @@schema("public")
}

model workspace_whiteboards {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String   @unique @db.Uuid
  yjs_state    String?  // base64-encoded Uint8Array from Y.encodeStateAsUpdate()
  updated_at   DateTime @default(now()) @updatedAt

  workspace workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

model workspace_channels {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workspace_id String   @db.Uuid
  name         String
  description  String?
  type         String   @default("PUBLIC") // PUBLIC, PRIVATE
  created_at   DateTime @default(now())
  updated_at   DateTime @updatedAt

  workspace workspaces           @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
  messages  workspace_messages[]

  @@unique([workspace_id, name])
  @@schema("public")
}

model workspace_messages {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  channel_id String   @db.Uuid
  sender_id  String   @db.Uuid
  content    String
  type       String   @default("TEXT") // TEXT, IMAGE, SYSTEM
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  channel workspace_channels @relation(fields: [channel_id], references: [id], onDelete: Cascade)
  sender  profiles           @relation(fields: [sender_id], references: [id], onDelete: Cascade)

  @@index([channel_id, created_at])
  @@schema("public")
}

// --- GLOBAL NOTIFICATION SYSTEM ---

model notifications {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id    String   @db.Uuid
  type       String // INVITE, SQUAD, COMMENT, SYSTEM
  title      String
  message    String
  link       String?
  is_read    Boolean  @default(false)
  created_at DateTime @default(now())

  user profiles @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id, created_at(sort: Desc)])
  @@index([user_id, is_read])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model squads {
  id                 String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  leader_id          String?              @db.Uuid
  workspace          workspaces?
  type               String
  title              String
  content            String
  tech_stack         String[]             @default([])
  capacity           Int?                 @default(4)
  recruited_count    Int?                 @default(1)
  place_type         String?              @default("online")
  location           String?
  activity_id        String?
  workspace_id       String?              @db.Uuid
  status             String?              @default("recruiting")
  recruitment_period String?
  created_at         DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?            @default(now()) @db.Timestamptz(6)
  squad_applications squad_applications[]
  squad_comments     squad_comments[]
  squad_members      squad_members[]
  profiles           profiles?            @relation(fields: [leader_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([status, created_at(sort: Desc)])
  @@index([type, created_at(sort: Desc)])
  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}
